#include <cstdio>
#include <cstring>

int main() {
    int t;
    std::scanf("%d", &t);
    int k, n;
    char l[100000];
    while(t--) {
        std::scanf("%d %d", &n, &k);
        std::scanf("%s", l);
    }
}